{
  "name": "pantheon-index",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "crawl": "node crawler/crawl.mjs",
    "smoke": "node scripts/smoke.mjs"
  }
}
